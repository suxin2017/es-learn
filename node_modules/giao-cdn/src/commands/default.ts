import { CommandHandler } from 'giao';
import { updateNotifier } from '@giao/cli-utils';
const config: CommandHandler = async (args, options, ctx) => {
    console.log(args, options);
    console.log('// to do, mr welcome');
    const pkg = require('../../package.json');
    updateNotifier(pkg, true);
};

export default config;
